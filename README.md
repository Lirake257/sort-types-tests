# sort-types-tests
generating differently sorted arrays with different N, save it to files, reading files and write it to dinamic array.
different sorts types c++ realization.
time measuring, measuring the number of conditions and swaps.
save the results to files.
one of test-result is here
